## Session 2026-05-13

### What Was Built
- Created a `.gitignore` file to exclude self-generated output directories from version control
- Configured exclusion of `.codex/` and `.claude/` directories to prevent tool artifacts from being committed
- Added Python-specific ignores (`__pycache__/` and `*.pyc`) for standard development hygiene

### Key Decisions
The decision to exclude both `.codex/` and `.claude/` directories indicates the project is designed to work with multiple AI coding assistants and wants to prevent their generated artifacts from polluting the repository. This is a clean separation between source code and tool-generated state.

### Files Changed
- `.gitignore` — Created new file with 5 lines excluding generated directories and Python cache files

### Lessons and Gotchas
This is foundational repository setup work. The explicit exclusion of both Codex and Claude output directories suggests prior experience with these tools leaving artifacts behind, making this a learned best practice for the project structure.